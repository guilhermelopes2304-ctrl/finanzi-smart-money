# FINANZZI Testing Standard

Maintain unit, integration, and Playwright end-to-end coverage appropriate to each feature. CI should gate typecheck, lint/format, unit tests, integration tests, E2E, build, coverage, and dead-code analysis. Use Codecov when coverage upload is configured. Introduce mutation testing progressively with Stryker/Stryke on critical logic as the test surface matures.
