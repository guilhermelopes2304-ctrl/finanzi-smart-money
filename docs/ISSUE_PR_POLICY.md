# Issue → PR → Preview Policy

All FINANZZI work follows: GitHub Issue → dedicated branch → Pull Request referencing the Issue → CI gates → preview/deploy → published-result verification → merge.

Do not implement corrections, improvements, or new features directly on `main`. Do not rewrite published history used by Lovable.
